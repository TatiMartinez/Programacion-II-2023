function suma(a: number, b: number): number {
    return a + b
};

//otra forma de definir una function"

const multi= (a:number, b:number):number => a * b; 

//otro tipo de funcion que valida si el parametro es valido, operador terniario

let firstCheck = true;
// condicion ? "true" : "false"

let access = firstCheck ? "Acceso Permitido": "Acceso Denegado";
console.log(access);
